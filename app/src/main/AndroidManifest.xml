<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahfuznow.androidarchitecture">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidArchitecture">
        <activity
            android:name=".mvp.view.MVPActivity2"
            android:exported="true" />
        <activity
            android:name=".mvp.view.MVPActivity1"
            android:exported="true" />
        <activity
            android:name=".mvp.view.MVPActivity"
            android:exported="true" />
        <activity
            android:name=".mvvm.MVVMActivity"
            android:exported="true" />
        <activity
            android:name=".mvc.view.MVCActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>